interface:
  display_name: "Say It Normally"
  short_description: "Plain, natural responses for any writing task"
  default_prompt: "Use $say-it-normally to write this response in plain, natural language."

policy:
  allow_implicit_invocation: true
