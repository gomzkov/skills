interface:
  display_name: "Say It Normally"
  short_description: "Clear, direct, natural responses"
  default_prompt: "Use $say-it-normally to make this response clear, direct, and natural."

policy:
  allow_implicit_invocation: true
